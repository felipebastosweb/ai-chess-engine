from .piece import *

class Rook(Piece):
    def move(pos_origin, pos_dest):
        if self.is_black:
            pass
        if not self.is_black:
            pass
    # Convert in str
    def __str__():
        return "R" if self.is_black else "r"
